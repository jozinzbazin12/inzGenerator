package generator.utils.filters;

public class ImageFilter extends AbstractFileFilter {

	public ImageFilter(String[] extensionList, String description) {
		super(extensionList, description);
	}
}
